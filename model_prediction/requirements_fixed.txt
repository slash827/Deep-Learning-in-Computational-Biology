# RNA-Protein Binding Prediction Dependencies
# PYTHON VERSION REQUIREMENT: Python 3.13 or higher
# These packages are tested and compatible with Python 3.13+
# 
# Installation instructions:
# 1. Ensure you have Python 3.13 or higher installed
# 2. Run: pip install -r requirements_fixed.txt

# Core PyTorch (CPU version for maximum compatibility)
torch>=2.6.0 --index-url https://download.pytorch.org/whl/cpu

# Numerical computing
numpy>=1.26.0
scipy>=1.11.0

# Transformer models and tokenization
transformers>=4.35.0
tokenizers>=0.15.0
huggingface-hub>=0.19.0
safetensors>=0.4.0

# Progress bars and utilities  
tqdm>=4.66.0

# Plotting (for debugging, not required for prediction)
matplotlib>=3.8.0

# HTTP and file handling
requests>=2.31.0
urllib3>=2.0.0
certifi>=2023.11.0
charset-normalizer>=3.3.0
idna>=3.6
filelock>=3.13.0

# Configuration and packaging
packaging>=23.2
pyyaml>=6.0.1
regex>=2023.12.0
typing-extensions>=4.8.0
